MONGO_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority"
PORT=5000

# CORS Configuration (comma-separated list of allowed origins)
# Example for production with Vercel:
# ALLOWED_ORIGINS="https://your-app.vercel.app,https://yourdomain.com,http://localhost:5173"
# Example for development:
# ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"
# If not set, defaults to common development ports (localhost:3000, localhost:5173, etc.)
# Note: Include all your frontend URLs (Vercel preview URLs, production URL, localhost for dev)

