MONGO_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority"
PORT=5000

# CORS Configuration (comma-separated list of allowed origins)
# Example for production with Vercel (include your actual Vercel URL):
# ALLOWED_ORIGINS="https://hrs-live.vercel.app,https://*.vercel.app,https://yourdomain.com,http://localhost:5173"
# 
# To allow all Vercel preview URLs, use wildcard: https://*.vercel.app
# 
# Example for development:
# ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"
# 
# If not set, defaults to common development ports (localhost:3000, localhost:5173, etc.)
# 
# IMPORTANT: Add your Vercel production URL to this list on Render!
# For Render: Go to Environment tab and add:
# ALLOWED_ORIGINS=https://hrs-live.vercel.app,https://*.vercel.app,http://localhost:5173

